# Architecture Notes

- Clean Architecture boundaries: domain, application, infrastructure, interfaces.
- AI services integrate via Strategy Pattern (classification, RAG, NRT scheduling).
- JWT stored in secure cookies. CSRF protection on state-changing routes.
